import React, { Component } from 'react';
import { Form, Button, Label, Segment, Header, Input } from 'semantic-ui-react';


class GameCreateFinal extends Component {

    render(){

   //  	console.log(`this.props.gameToCreate: `, this.props.gameToCreate);
   //  	const estimators = this.props.gameToCreate.map(estimator => {
	  // 		return (
	  // 			<div key={estimator._id}>
		 //        <Input label="Estimator Username:" type='text' name='username' value={estimator.username}/>
		 //        <Input label="Estimator Email:" type='text' name='email' value={estimator.email}/>
	  //       </div>
			// )
   //  	})

        return(
        	<Segment>
	        	<Header as="h1">GameCreateFinal</Header>

	        	<Form>
	        
	        	</Form>
        		
        	</Segment>
            
        )
    }
}
export default GameCreateFinal;
